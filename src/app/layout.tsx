import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Arabic Dialogue Corpus - Generate Authentic Arabic Conversations",
  description: "Generate realistic Arabic dialogues for language learning, cultural understanding, and educational purposes using AI-powered conversation generation.",
  keywords: ["Arabic", "dialogue", "language learning", "conversation", "Arabic corpus", "language education"],
  authors: [{ name: "Arabic Dialogue Corpus" }],
  openGraph: {
    title: "Arabic Dialogue Corpus",
    description: "Generate authentic Arabic conversations for language learning and cultural understanding",
    type: "website",
    locale: "en_US",
  },
  robots: {
    index: true,
    follow: true,
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body 
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        suppressHydrationWarning
      >
        {children}
      </body>
    </html>
  );
}